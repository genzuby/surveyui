import { FETCH_SURVEY_LIST } from "../actions";
import surveyReducer from "../reducers";

describe("Survey Reducer", () => {
  it("Should return data from reducer", () => {
    // const newState = surveyReducer(undefined, {
    //   type: FETCH_SURVEY_LIST ,
    //   payload: posts
    // });
    // expect(newState).toBe(1);
  });
});
